# applicaton-dev.yml
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: ENC(IxWZd7iLU/V9qi8AT63ALcmAhRWD3TKqOQQPv0wNRtltexkp0AWaRCyvZ1CwG9tWck0qdRO1WRs=)
    username: ENC(02RRfWWBpyqVy0Sp725Oyg==)
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

# ${환경변수} 로 숨기기
# jasypt.encryptor.password=${JASYPT_ENCRYPTOR_PASSWORD}
# 1. Run/Debug Configurations를 열기
# 2. 애플리케이션을 선택한 후, 환경 변수 부분에 JASYPT_ENCRYPTOR_PASSWORD와 값(NAME, VALUE)을 추가
jasypt:
  encryptor:
    password: encryptTestKey



logging.level:
  org.hibernate.SQL: debug
  # org.hibernate.type: trace

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /v3/api-docs
